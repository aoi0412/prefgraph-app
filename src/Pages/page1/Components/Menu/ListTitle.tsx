import { FC } from 'react';
import Text from '../../../../Components/Text';

const ListTitle: FC = () => (
  <div style={{ padding: '4px', paddingLeft: '12px', margin: 0 }}>
    <Text size="lg" bold>
      都道府県
    </Text>
  </div>
);

export default ListTitle;
